{
  "name": "desafio",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /Desafio-5",
    "deploy": "gh-pages -d build"

  },
  "devDependencies": {
    "parcel": "^2.7.0"
  },
  "dependencies": {
    "@types/node": "^18.11.7",
    "gh-pages": "^4.0.0"
  }
}
